type EventTicket {
  id: ID!
  eventModel: EventModel!
  owner: User!
}

input EventTicketInput {
  eventModelId: ID!
}

type Mutation {
  saveEventTicket(eventTicket: EventTicketInput): EventTicket
}
