type EventTicket {
  id: ID!
  eventSession: EventSession!
  owner: User!
}

type Mutation {
  createEventTicket(eventTicketInput: EventTicketInput): EventTicket
  deleteEventTicket(eventTicketInput: EventTicketInput): Boolean
}

input EventTicketInput {
  eventSessionId: ID!
}
