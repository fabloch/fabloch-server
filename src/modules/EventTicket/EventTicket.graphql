type EventTicket {
  id: ID!
  eventSession: EventSession!
  owner: User!
}

type Mutation {
  createEventTicket(eventTicket: EventTicketInput): EventTicket
}
input EventTicketInput {
  eventSessionId: ID!
}
