type Event {
  id: ID!
  title: String!
  owner: User!
}

input EventInput {
  title: String
}

type Query {
  allEvents: [Event!]
  eventDetail(id: String!): Event!
}

type Mutation {
  createEvent(event: EventInput): Event
}
