type Newcomer {
  id: ID!
  email: String!
  digits: String!
}

input NewcomerInput {
  email: String!
}

type Query {
  newcomer: Newcomer
}

type Mutation {
  createNewcomer(newcomer: NewcomerInput): Newcomer
}
